name: GAUwallet CI/CD

on:
  # Automatic: Run on every push to main
  push:
    branches: [ main ]

  # Manual triggers with action selection
  workflow_dispatch:
    inputs:
      action:
        description: 'Action to perform'
        required: true
        type: choice
        options:
          - 'integration-tests'
          - 'build-android'
          - 'build-ios'

env:
  FLUTTER_VERSION: '3.38.5'
  JAVA_VERSION: '17'

jobs:
  # ============================================
  # AUTOMATIC: Run on every push to main
  # ============================================
  analyze-and-test:
    name: Analyze and Test
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    timeout-minutes: 20

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🐦 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: 'stable'
          cache: true

      - name: 📦 Install dependencies
        run: flutter pub get

      - name: 🔍 Run flutter analyze
        run: flutter analyze --no-fatal-infos --no-fatal-warnings

      - name: 🧪 Run unit tests (exclude integration)
        run: flutter test --exclude-tags=integration

      - name: 🔨 Build Android AAB (debug - verification only)
        run: flutter build appbundle --debug

      - name: ✅ Success
        run: |
          echo "✓ Flutter analyze passed"
          echo "✓ Unit tests passed"
          echo "✓ Android build verified"

  # ============================================
  # AUTOMATIC: Deploy Web to Vercel
  # ============================================
  deploy-web:
    name: Deploy Web to Vercel
    runs-on: ubuntu-latest
    needs: analyze-and-test
    if: github.event_name == 'push'
    timeout-minutes: 15

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🐦 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: 'stable'
          cache: true

      - name: 📦 Install dependencies
        run: flutter pub get

      - name: 🔨 Build Flutter Web (Release)
        run: flutter build web --release

      - name: 🚀 Deploy to Vercel (Production)
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          vercel-args: '--prod'
          working-directory: ./build/web

      - name: ✅ Deployment Complete
        run: |
          echo "╔══════════════════════════════════════════════════════════╗"
          echo "║  ✅ WEB APP DEPLOYED TO VERCEL SUCCESSFULLY             ║"
          echo "╚══════════════════════════════════════════════════════════╝"
          echo ""
          echo "🌐 Production URL: https://wallet-v2.vercel.app"
          echo "   (or your custom domain if configured)"

  # ============================================
  # MANUAL: Integration Tests (Mainnet)
  # ============================================
  integration-tests:
    name: Integration Tests - Production Backend
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'integration-tests'
    timeout-minutes: 15

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: ⚠️ Mainnet Warning
        run: |
          echo "╔══════════════════════════════════════════════════════════╗"
          echo "║  ⚠️  WARNING: Running tests on PRODUCTION mainnet       ║"
          echo "║  This will cost real gas (~$0.02-0.05 total)           ║"
          echo "║  Backend: https://metatx.vercel.app                    ║"
          echo "║  RelayerV4: 0xA7E2f9aF0023CF4558Baac747Dd01179297dDE8D ║"
          echo "╚══════════════════════════════════════════════════════════╝"
          echo ""

      - name: 🐦 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: 'stable'
          cache: true

      - name: 📦 Install dependencies
        run: flutter pub get

      - name: 🔥 Run Critical Path Tests
        run: flutter test test/integration/critical_path_test.dart

      - name: ✅ Integration Tests Passed
        run: |
          echo "╔══════════════════════════════════════════════════════════╗"
          echo "║  ✅ ALL INTEGRATION TESTS PASSED                        ║"
          echo "║  Production backend is READY for deployment             ║"
          echo "╚══════════════════════════════════════════════════════════╝"

  # ============================================
  # MANUAL: Build Android Release AAB
  # ============================================
  build-android:
    name: Build Android Release AAB
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'build-android'
    timeout-minutes: 30

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: ☕ Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: ${{ env.JAVA_VERSION }}

      - name: 🐦 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: 'stable'
          cache: true

      - name: 📦 Install dependencies
        run: flutter pub get

      - name: 🔨 Build Release AAB
        run: flutter build appbundle --release

      - name: 📤 Upload Release AAB
        uses: actions/upload-artifact@v4
        with:
          name: app-release.aab
          path: build/app/outputs/bundle/release/app-release.aab
          retention-days: 90

      - name: 🚀 Upload to Google Play (Internal Track)
        uses: r0adkll/upload-google-play@v1
        with:
          serviceAccountJsonPlainText: ${{ secrets.GOOGLE_PLAY_SERVICE_ACCOUNT_JSON }}
          packageName: com.gaugecash.wallet
          releaseFiles: build/app/outputs/bundle/release/app-release.aab
          track: production
          status: completed

      - name: ✅ Build Complete
        run: |
          echo "╔══════════════════════════════════════════════════════════╗"
          echo "║  ✅ ANDROID RELEASE AAB BUILT SUCCESSFULLY              ║"
          echo "║  ✅ UPLOADED TO GOOGLE PLAY (PRODUCTION)                ║"
          echo "╚══════════════════════════════════════════════════════════╝"
          echo ""
          echo "📦 AAB file location:"
          echo "   build/app/outputs/bundle/release/app-release.aab"
          echo ""
          echo "🚀 Deployed to:"
          echo "   - Google Play Console (Production)"
          echo "   - GitHub Actions Artifacts (90 day retention)"
          echo ""
          echo "📝 Next steps:"
          echo "   1. Monitor Google Play Console for rollout status"
          echo "   2. Check user reviews and crash reports"
          echo "   3. App will be live to 100% of users"

  # ============================================
  # MANUAL: Build iOS Release IPA
  # ============================================
  build-ios:
    name: Build iOS and Deploy to TestFlight
    runs-on: macos-latest
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'build-ios'
    timeout-minutes: 60

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🐦 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: 'stable'
          cache: true

      - name: 📦 Install dependencies
        run: flutter pub get

      - name: 💎 Setup Ruby and Fastlane
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.0'
          bundler-cache: false

      - name: 📦 Install Fastlane and CocoaPods
        run: |
          gem install fastlane
          cd ios && pod install

      - name: 🔑 Configure SSH for Match
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.MATCH_GIT_PRIVATE_KEY }}

      - name: 🌐 Add GitHub to Known Hosts
        run: ssh-keyscan -t rsa github.com >> ~/.ssh/known_hosts

      - name: 🔨 Build and Sign IPA with Match
        run: |
          cd ios
          fastlane build_signed
        env:
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
          MATCH_GIT_URL: ${{ secrets.MATCH_GIT_URL }}
          APP_STORE_CONNECT_KEY_ID: ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
          APP_STORE_CONNECT_API_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_API_ISSUER_ID }}
          APP_STORE_CONNECT_API_KEY_CONTENT: ${{ secrets.APP_STORE_CONNECT_API_KEY }}

      - name: 🚀 Upload to TestFlight
        run: |
          cd ios
          fastlane beta
        env:
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
          MATCH_GIT_URL: ${{ secrets.MATCH_GIT_URL }}
          APP_STORE_CONNECT_KEY_ID: ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
          APP_STORE_CONNECT_API_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_API_ISSUER_ID }}
          APP_STORE_CONNECT_API_KEY_CONTENT: ${{ secrets.APP_STORE_CONNECT_API_KEY }}

      - name: 📤 Upload IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: GAUGECASH-ios.ipa
          path: ios/build/GAUGECASH.ipa
          retention-days: 90

      - name: ✅ Deployment Complete
        run: |
          echo "╔══════════════════════════════════════════════════════════╗"
          echo "║  ✅ iOS APP SUBMITTED TO APP STORE FOR REVIEW           ║"
          echo "╚══════════════════════════════════════════════════════════╝"
          echo ""
          echo "📦 IPA file location:"
          echo "   ios/build/GAUGECASH.ipa"
          echo ""
          echo "🚀 Deployment status:"
          echo "   - Uploaded to App Store Connect"
          echo "   - Submitted for Apple Review (automatic)"
          echo "   - Will auto-release after approval (1-2 days)"
          echo "   - GitHub Actions Artifacts (90 day retention)"
          echo ""
          echo "📝 Next steps:"
          echo "   1. Monitor App Store Connect for review status"
          echo "   2. Check email for any review feedback from Apple"
          echo "   3. App will go live automatically after approval"
